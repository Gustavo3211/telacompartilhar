import { defineConfig } from 'vite';

export default defineConfig({
  // Set base path for GitHub Pages deployment.
  // Relative path './' works for any repo name, or you can specify '/repo-name/'
  base: './',
});
